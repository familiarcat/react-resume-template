version: 1
backend:
  phases:
    preBuild:
      commands:
        - npm install -g npm@9.8.1
        - nvm install 18.19.0
        - nvm use 18.19.0
        - npm --version
        - node --version
    build:
      commands:
        - npm ci --legacy-peer-deps
        - npm run generate-env
frontend:
  phases:
    preBuild:
      commands:
        - npm install -g npm@9.8.1
        - nvm install 18.19.0
        - nvm use 18.19.0
        - npm --version
        - node --version
        - npm ci --legacy-peer-deps
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - .next/cache/**/*
